import 'package:flutter/material.dart';

class PageWork extends StatelessWidget {
  const PageWork({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('はたらく画面', style: TextStyle(fontSize: 24)),
    );
  }
}