import 'package:flutter/material.dart';

class PageAccount extends StatelessWidget {
  const PageAccount({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('アカウント画面', style: TextStyle(fontSize: 24)),
    );
  }
}