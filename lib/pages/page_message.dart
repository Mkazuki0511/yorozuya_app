import 'package:flutter/material.dart';

class PageMessage extends StatelessWidget {
  const PageMessage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('メッセージ画面', style: TextStyle(fontSize: 24)),
    );
  }
}