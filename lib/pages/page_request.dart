import 'package:flutter/material.dart';

class PageRequest extends StatelessWidget {
  const PageRequest({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('いらい画面', style: TextStyle(fontSize: 24)),
    );
  }
}